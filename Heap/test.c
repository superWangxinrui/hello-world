#define _CRT_SECURE_NO_WARNINGS
#include "Heap.h"
int main(){
	//int arr[] = { 53, 17, 78, 9, 45, 65, 87, 23, 31 };
	//Heap Hp;
	//int sz = sizeof(arr) / sizeof(arr[0]);
	//CreateHeap(&Hp, arr,sz,greater);//创建小堆
	////InsertHeap(&Hp, 5);//插入5
	////delect(&Hp);//删除堆顶元素
	//int ret = Heapsize(&Hp);
	//for (int i = 0; i <(&Hp)->size; i++){
	//	printf("%d ", (&Hp)->array[i]);
	//}
	//printf("\n");
	//printf("堆的元素个数为%d", ret);
	//test();
	test1();
	system("pause");
}